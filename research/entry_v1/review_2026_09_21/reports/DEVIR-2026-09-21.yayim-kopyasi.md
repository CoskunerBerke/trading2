# DEVİR — 2026-09-21

`trading2` PAPER kripto botu. **Gerçek para YOK.** OVH VPS'te 7/24 çalışıyor.
Kullanıcı Türkçe konuşuyor. VPS komutlarını **kullanıcı çalıştırır** (SSH anahtarı passphrase'li).

> **Bu belgeyi kanıt sayma.** Her iddianın yanında onu yeniden türeten komut var.
> İlk iş §1'deki doğrulamayı koştur. Sapma varsa ÖNCE onu anla.

---

## 0. En önemli iki şey

1. **Binance yerelden erişilebilir** — `https://www.binance.info` açık, aynı `/fapi/v1/*` uçlarını
   veriyor (klines 12 sütun, funding, exchangeInfo). `fapi.binance.com` vb. yedi host TCP'de
   sıfırlanıyor. **VPS'e ihtiyaç yok.** Önceki devirlerdeki "VPS kullan" notu yanlıştı.
2. **Kâr kuyrukta.** Her pencerede en iyi 3 işlem net kârın %100'ünden fazlası. Kuyruğa dokunan
   her mekanizma (başa-baş, trail, kısmi kâr) değer yok ediyor; seçimi değiştiren her şey
   (sıralama, evren) tek-işlem gürültüsü. **Ayar aramaya devam etme** — altı deneme düştü.
   Tek çalışan kaldıraç: **bakiye**.

---

## 1. Durum doğrulaması (ilk iş)

```bash
cd C:/Users/berke/wt-entry && git status --porcelain && git rev-parse --short HEAD
```
Beklenen: HEAD `3b0ae8e`, dal `work/box-theory-v15`, **10 değişik + 7 yeni test dosyası**,
527 ekleme. **Commit EDİLMEDİ, VPS'e DAĞITILMADI.**

```bash
cd C:/Users/berke/wt-entry && python -m pytest tests/ -q --tb=short
```
Beklenen: **2610 passed, 22 skipped, 0 failed** (son ölçüm config şeması eklenmeden önce 2610;
şema eklemesinden sonra hedefli 229/229 yeşil). **Koşu sürerken ağaca dokunma** — bkz. §6.

```bash
python -c "import urllib.request,json;r=urllib.request.urlopen(urllib.request.Request('https://www.binance.info/fapi/v1/klines?symbol=BTCUSDT&interval=1d&limit=3',headers={'User-Agent':'Mozilla/5.0'}),timeout=20);print(len(json.load(r)),'bar')"
```
Beklenen: `3 bar`.

VPS (kullanıcı koşturur):
```bash
ssh -t <VPS_KULLANICI>@<VPS_HOST> "sudo /opt/tradingbot/venv/bin/python - <<'PY'
import json, io, subprocess
print('HEAD:', subprocess.run(['git','-C','/opt/tradingbot/app','rev-parse','--short','HEAD'], capture_output=True, text=True).stdout.strip())
for key, ad in (('strategy_paper','T2'),('strategy_paper_m2','M2'),('strategy_paper_box','BOX'),('pattern_trader','FORMASYON')):
    d = json.load(io.open('/opt/tradingbot/data/state/%s.json' % key, encoding='utf-8'))
    c = d.get('counters') or {}; s = d.get('summary') or {}
    print('%-10s ozkaynak=%-8s acilan=%-4s kapanan=%-4s' % (ad, s.get('equity'), c.get('opened'), c.get('closed')))
PY"
```
VPS hâlâ `3b0ae8e`. Son görülen (2026-09-20, ekran): M2 110,6 · T2 103,3 · Mum 97,7 ·
Ana bot 93,0 · Box 86,6.

---

## 2. YARIM KALAN İŞLER — KOŞMUYOR, yeniden başlatılmalı

Arka planda başlatılan iki iş **öldü** (2026-09-21 doğrulandı: trail 7/18, 5m 6/40, ilerleme yok).

| İş | Durum | Yeniden başlat |
|---|---|---|
| Trail ölçümü | **7/18** (11 koşu config hatasıyla düştü) | aşağıda |
| 5m arşiv | **6/40** coin | aşağıda |

```bash
cd C:/Users/berke/research/entry_v1 && python C:/Users/berke/trading2-deploy/olcumler/trail.py
```
```bash
cd C:/Users/berke/research/entry_v1 && python collect_binance.py --dest "C:/Users/berke/research/bn_archive" --timeframes 5m --from 2024-08-01 --to 2026-09-21 --no-funding --out "C:/Users/berke/research/entry_v1/out/collect_5m.json"
```
İkisi de sürdürülebilir: trail başarılı `run_id`'leri atlar, 5m toplayıcısı idempotent.
`trail.json`'da aynı `run_id` hem başarısız hem başarılı olabilir — analizde `ok=True` olanı al.
**İkisi koşarken `wt-entry` kodu ve `config.yaml` DÜZENLENMEZ** (bkz. §6).

## 3. KOD DEĞİŞİKLİKLERİ — hepsi test + geri alma sondası ile

### Kusur onarımları (AÇIK — davranışı düzeltir)
| Dosya | Ne |
|---|---|
| `ema200_trend.py` | Kural çıkışı, formülünde yer almayan ATR14'e bağlıydı; ölçülemezse pozisyon çıkışı bir daha alamıyordu. M2 29 bar yeterken 210 istiyordu. Artık `exit_measure` ayrı, ölçülemezse `EXIT_UNMEASURABLE`. **Canlıda tetiklenmiyor** (VPS'te ölçüldü: 4 tutulan sembol de 399 bar) — koruyucu. |
| `futures_ledger.py` | Likidasyon, stop **daha yakın** olsa bile önce tetikleniyordu. 1x'te uykuda; 2x'te 2 işlem −1R yerine −1,40R. |
| `strategy_paper.py` | Tutulan pozisyonun `last_actions`'ı donuyordu; kural döngüsü tazeliği (`rule_evaluated_at`, `rule_stale_s`, `rule_evaluated_this_write`) ayrı ilan ediliyor — 60 sn izleyici defteri "canlı" gösteremez. |
| `replay/engine.py` | Eksik arşivli sembol sessizce düşüyordu, meta İSTENEN listeyi yazıyordu → `loaded_symbols`/`skipped_symbols` + `require_all`. |
| `config_v3.py` | `max_entry_drift_pct` şemada yoktu → config yükleyici **sessizce yok sayıyordu**. |
| `test_chart_analysis_v1_fixes.py` | **Aralıklı test çözüldü:** `NOW` import'ta donuyor, üretim istek anındaki saate bakıyor. 330 sn gecikmeli sondayla kanıtlandı. |
| `test_strategy_paper_v1.py` | Sözleşme testi yorum satırındaki ismi kod sanıyordu → AST tabanlı. |

### Yeni mekanizmalar (VARSAYILAN KAPALI — config ile açılır)
| Mekanizma | Kapalıyken | Açılması |
|---|---|---|
| **İhtiyaç kadar kaldıraç** (`leverage_max`) | sabit kaldıraç | taban→tavan, **yalnız gerektiği kadar** |
| **Giriş kayması kapısı** (`max_entry_drift_pct`) | kapı yok | kuralın fiyatından %X kaymış girişi reddeder (`ENTRY_DRIFT`) |
| **MFE eşikli trail** (`trail_arm_r`, `trail_dist_r`) | trail yok | MFE eşiği aşılınca silahlanır |
| **Aday sıralama** (replay `candidate_order`) | arşiv sırası | yalnız araştırmada; canlıya ALINMADI |

### Config (`config.yaml`) — kullanıcı talimatıyla
| Defter | Bakiye | Kaldıraç taban/tavan | Kayma kapısı |
|---|---|---|---|
| T2 | 100 → **200** | 1 → **2 / 4** | 0 (kapalı) |
| M2 | 100 → **200** | 1 → **2 / 4** | 0 (kapalı) |
| Box | 100 → **200** | 3 → **3 / 4** | tanımsız (kapalı) |

Kayma kapısı **bilerek kapalı**: mekanizma testli ama eşik değeri ölçülmedi.

---

## 4. ÖLÇÜM SONUÇLARI (`trading2-deploy/olcumler/`)

| Deney | Hüküm |
|---|---|
| **40 coin vs 10 coin** (`run40.json`) | 2 lehte / 4 aleyhte; M2 bileşik 10c +216% vs 40c +100%. **Ama fark büyük ölçüde tek işlem** (P1'de tek SOL +23,2) |
| **Sıralama** (`rank40.json`, `grid.json`) | 6/6 hücrede CI sıfırı içeriyor. **Kanıtlanmadı, canlıya alınmadı** |
| **Slot sayısı** (`slots.json`) | 12 slotta MIN_ORDER_CONFLICT 14906, açılan 36→17. **100 USDT'de yapılamaz** |
| **Kaldıraç × bakiye** (`lev.json`) | Kaldıraç bağlayan kısıtı açmıyor (MAX_POSITION_PCT 1-14 red). **Bakiye 200: MIN_ORDER_CONFLICT 587→0** |
| **Başa-baş** (`be_measure.json`) | Getiri 3-3, maks düşüş 6/6 kötü. **Eklenmedi** |
| **Trail** (`trail.json`) | M2 P3 107→59 (**bozuyor**), T2 P3 −4,8→+43,7 (düzeltiyor). 11 koşu eksik |

Kaldıraç likidasyonu (620 kapanış): L2 **0/620** meşru likidasyon · L3 9 · L5 54.

---

## 5. SIRADAKİ İŞ

1. **Trail'in kalan 11 koşusunu bitir** ve kural başına hüküm ver. Beklenti: M2'de kapalı kalmalı,
   T2'de açılabilir. Tek pencereye (P3) bakarak karar verme — altı pencerenin hepsini gör.
2. **5m arşiv bitince box'u ölç** — dört kol: onarımsız / kayma kapısı / kaldıraç tavanı / ikisi.
   Box'un kök kusuru ölçüldü: kural girişi 5m bar kapanışında hesaplıyor, defter 15 dk sonraki
   markla açıyor; boyut yeni fiyatı eski stopla kullandığı için seçim **yönlü** (yalnız "hareket
   zaten olmuş" girişler süzülüyor). **Uyarı:** 11 kapanışta 2 kazanan için P(X≤2)≈0,09 —
   isabet açığının kendisi istatistiksel olarak KURULMADI; mekanizma kanıtlı, büyüklüğü değil.
   Box sweep için `box_sweep.py` ve `run_box_v15.py` (`research/entry_v1`).
3. **Dağıtım** — kullanıcı onaylarsa. Paket: yukarıdaki değişiklikler. Bundle **dal ref'iyle**
   üretilmeli (`git bundle create <dosya> <taban>..<DAL> <DAL>`), HEAD ile değil.

---

## 6. Bu oturumda pahalıya öğrenilenler

* **Tek host'un engelli olması "kapalı" demek değil.** Yedi host denendi, biri açıktı.
* **Ölçüm/test koşarken kod veya config düzenleme** — üç kez yanıldı. Önce bitir, sonra koştur.
  Uzun koşuları **kalıcı dizine** yazdır; scratchpad oturuma özel.
* **Kesilmiş arama sonucu tam sonuç değildir** (`head -20` bir alanı "ölü" gösterdi).
* **Ölçümden türetilen bayrak, ölçüm gürültüsünü davranışa çevirir** — olguyu taşı.
* **Kıtlık yoksa sıralama ölçülemez** (3 aday / 3 slot testte hiçbir şey kanıtlamıyordu).
* **Aynı bardaki açılışlar aynı zaman damgasını taşır** — uygulama sırası `_entry_meta` ekleme
  sırasındadır, zaman sıralaması alfabetiğe düşer.
* **Her karşılaştırmada en iyi işlemi çıkarıp tekrar bak.** Bu projede getiri tek başına yanıltıcı.
* **Sayı üretmeden önce yaklaşımı çatışmalı denetle** — TradingView arşivi hazırdı, denetim
  funding'in ve borsa kurallarının olmadığını yakaladı.
* **Kendi yanlışını hemen söyle.** Bu oturumda üç iddia geri alındı (40 coin üstünlüğü,
  sıralamanın sağlamlığı, kaldıracın işlem sayısını artıracağı); hepsi ölçümle.

## 7. Hafıza
`C:\Users\berke\.claude\projects\C--Users-berke-Trading-bot\memory\` — özellikle:
`tail-concentration-2026-09-21` · `no-edit-during-measurement` · `binance-endpoints-blocked-locally` ·
`forty-coin-archive-solved` · `paper-exit-audit-2026-09-19` · `flaky-panel-mark-test` ·
`handoff-verification-protocol`
